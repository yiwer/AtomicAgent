# Ticket06 implementation evidence

Baseline: 1f6c7462fe306c43656b0499508fbf34ea0b907d. Implement/TDD seams are the approved HTTP API, SDK QueryPort, OpenSandbox HTTP, durable storage fault seam and real Chromium A.

TDD: first API test failed because bindings.skills was absent; implementation green at 1/1. SDK boundary first failed module resolution, then passed controlled plugin discovery and hook evidence. Journal-write fault test failed with runtime_failed and SDK-swallowed hook semantics, then passed after explicit deny/abort and sticky required_capability_failed. Subsequent negative and recovery cases validate no evidence -> no success.

Focused current: `npx tsx --test tests/skills.test.ts tests/claude-skills.test.ts tests/opensandbox-skills.test.ts` = 24/24 (API7, QueryPort12, actual OpenSandbox SDK local HTTP5). `npm run typecheck` passed. Browser focused earlier 1/1; final full count recorded below after final verification.

Evidence layers: HTTP/SQLite/blob/process-data file operations and Chromium are real local execution. Claude query and resource creation are deterministic external doubles. OpenSandbox protocol tests use the real installed SDK against a local HTTP fixture; they prove bounded matching evidence import on command failure, not Linux resource or model behavior. Final model/profile, Docker/gVisor isolation and human acceptance remain NOT_EXECUTED.

No external endpoint/model, credential variable binding, purpose/billing approval, Linux node or immutable image was supplied. No paid calls, unrelated credential reads or remote deployments occurred. AC checkboxes remain open.

Checkpoint verification: `npm test` 78/78; `npm run test:browser` 14/14; `npm run typecheck` and `npm run build` passed. `npx tsx scripts/verify-local.ts` compiled-entrypoint restart and configuration recovery checks passed in fixture mode. Mobile screenshot now scrolls the modal to Skill evidence; real Chromium confirms no horizontal overflow. The four Ticket05 screenshots regenerated by the full suite were restored to preserve the preceding ticket's evidence.
