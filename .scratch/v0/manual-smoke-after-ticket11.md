# Ticket11 后的人工 smoke 与体验验收

状态：云端人工入口已部署，等待用户体验。按用户 2026-09-14 指令，完成 Ticket11 后停止自动推进，不启动 Ticket12；下列人工结果当前全部未执行。

2026-09-14 云端入口更新：使用 **[https://agent.91boy.cn](https://agent.91boy.cn)**，工作区 `cloud-smoke`，环境 `json-default@1`。从私有文件 `.local/cloud-deploy/config.json` 复制 `identities[0].token` 登录。云端已完成 HTTPS／文件与产物／回滚持久性／浏览器的工程检查，仍为明确的 fixture，不调用真实模型，不创建任务 Docker 沙箱；[部署与 CI 说明](../../docs/cloud-deployment.md)。下面保留的 4312 地址、令牌位置与 Run ID 是原本机预检记录，不能用于云端登录。

运行代码：`9162199`。入口：[http://127.0.0.1:4312](http://127.0.0.1:4312)。使用正常 `main`、真实 API 与 SQLite，独立保存本轮 smoke 数据。本机已有 4310 服务，本轮使用独立端口和数据库。

打开私有配置 [config.json](../../.local/manual-smoke-ticket11/config.json)，复制 `identities[0].token` 到登录框。工作区为 `manual-smoke-ticket11`，环境为 `json-default@1`，默认期限 3600 秒；数据库在同目录的 `runs.db`。访问令牌不写入本清单、日志或提交。

当前实例已经运行。以下命令仅供实例停止后，在仓库根目录用 PowerShell 自行重启：

```powershell
npm run build
$env:ATOMIC_CONFIG = Join-Path (Get-Location) '.local/manual-smoke-ticket11/config.json'
npm start
```

使用配置中的访问令牌登录控制台。终止自行启动的前台实例时按 Ctrl+C，等待应用正常退出并释放数据库锁；保留数据库作为本轮验收记录。

启动预检已留下两条任务，便于查看：超额失败 `3a669a38-273f-4b5f-b80d-9dbdf0da1c43`；恢复额度后成功 `961189fc-e962-4d5e-887c-dc86647ecb6a`。策略已恢复全部默认值，当前修订为 3。请另建自己的 Run 记录人工体验；这些预检任务不算用户验收。

## 本轮范围

先体验已经交付的任务、文件、配置、取消和限额流程，核对页面信息、刷新持久性及错误恢复。执行来源会明确显示：本机 fixture 可以验证 UI/API/持久化流程；实际 Linux/provider/资源限制使用对应票的独立证据。未配置获准模型前，不把本轮体验记为真实模型推理验收。gVisor、生产容量和未交付的后续功能也不在本次通过结论内。

## 可复用输入

下载或在文件选择器中选择 [manual-smoke-input.csv](manual-smoke-input.csv)。契约为 `id,category,value`，四行输入包含一行非法数值。

预期文件统计：输入 4 行、有效 3 行、拒绝 1 行；`total` 为字符串 `12.6`，分组 `alpha=7.3`、`beta=5.3`。成功后应能下载有效 CSV 与被拒绝行 JSON；任务资源回收与文件下载分别核对。

准备阶段已用 Node 24.18.0 下的编译后文件处理程序核对样例结果一致，并删除该次临时处理目录；这项输入预检不代替下表的人工体验结果。

## 人工记录

| 场景 | 操作与观察重点 | 人工结果 |
| --- | --- | --- |
| 登录与导航 | 进入交付地址，核对版本、工作区、执行来源；已开放入口可用，未交付入口不冒充可用 | 未执行 |
| 首个任务 | 提交小型 JSON 任务，核对唯一 Run、状态与已校验结果；刷新后仍是同一任务 | 未执行 |
| 文件处理 | 上传上述 CSV，核对计数、精确总数、分组与两个产物；回收后仍能下载 | 未执行 |
| 产物复用 | 从仍有效的产物创建新任务，核对来源和独立输入副本；原任务与产物不被改写 | 未执行 |
| 配置与冻结依据 | 查看或发布可用修订，核对变更预览、旧任务清单不变及刷新持久性 | 未执行 |
| 取消 | 存在运行中任务时点击“取消任务”，分别观察取消裁定、实际停止和独立回收；若小样例已完成则保留本项未执行 | 未执行 |
| Ticket11 限额 | 侧栏“额度与限制”修改登记范围内的数值，填写“限额调整理由”，依次“预览限额变更”“确认发布限额”；刷新后修订保留，新任务冻结新值，旧清单保持原值 | 未执行 |
| 错误恢复与手机布局 | 核对错误可读、刷新后可恢复；窄窗口中主要操作及详情可读，无横向溢出 | 未执行 |

请记录发现问题时的 Run ID、具体操作、预期与实际表现。整体反馈可以直接写“smoke 通过”或列出问题；有未执行项时请保留未执行，不默认全部验收完成。用户明确反馈前，自动化和工程师目视不代填人工结果。

本机正常 fixture 的小任务通常很快结束，不能保证人工操作时仍能看到运行中取消或持续排队。本轮先核对可稳定重复的任务、文件、复用、配置与限额流程；取消、排队和执行层故障仍分别保留对应自动化／Linux 证据与未完成的人工结果。

## 限额体验的观察点

先保留一个已完成任务，再将并发从 2 调整为 1，并填写可辨认的变更说明。预览应显示新旧数值与对既有任务的影响，确认后出现新修订；刷新页面并创建新任务，对比两份冻结清单。该操作验证发布和持久化，不单独证明真实 CPU／内存限制。

可重复的超额路径：将“产物总量（bytes）”改为 `1` 并预览发布，再用四行 CSV 创建文件任务。预期任务以 `budget_exceeded` 失败，限制维度为 `artifact_bytes`，不会出现成功 Result；独立查看停止和回收状态。该本机路径验证产物交付限制，不证明 Linux cgroup 或内核磁盘限制。完成后把产物总量恢复为 `104857600`，再次预览发布，用新的 Run 重做文件成功场景。

2026-09-14 入口预检：正常 `main` 的会话登录、额度发布、超额失败（`stopped` / `complete`）、恢复后成功、两项可用产物及样例统计全部通过；记录在私有目录的 `launch-preflight.json`。此前临时 `createApp` 实例预检也已完成并清理；其首轮脚本误把有限等待终态的 HTTP 200 判为应返回 202，修正脚本后通过，未修改产品行为。用户人工执行仍未开始。

任务详情的“实际限制观测”展示执行层证据；fixture 的观测为 null，应明确提示没有物理证明。本票最终 Linux 实验使用 `ce8d0083` 镜像；[压力记录](evidence/ticket11/physical/pressure-host.jsonl)、[全控制面故障记录](evidence/ticket11/physical/crash-host.jsonl)、[资源账本](evidence/ticket11/physical/resource-ledger.json) 与 [清理记录](evidence/ticket11/cleanup.jsonl) 分别提供物理证据，不替代本表人工结果。

发布发生网络中断时，页面保留原请求身份，可用“找回未确认限额发布”核对原操作结果。不要为了测试恢复而清除会话存储或重建请求。
