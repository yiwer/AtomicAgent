# 10 — 让恶意任务在隔离与只读边界内失败

**What to build:** 文件或研究输入试图越界时，平台实际阻断并返回受控失败，A 展示可核对原因；后续任务仍获得干净执行空间。

**Blocked by:** 07 — 通过登记只读 MCP 交付研究报告

**Status:** ready-for-agent

**范围来源：** PRD + A；2026-09-14 用户接受 v0＋A 拆分后发布。

**验收对应：** AC-04、AC-15。

## Acceptance criteria

- [ ] 在工具、凭据、文件和网络边界联合执行只读／任务工作区策略；覆盖路径与符号链接越界、重定向、不允许目的地和外部写。
- [ ] A 写标记及启动子进程后，B 不继承文件、进程、配置、凭据或会话；终态后不能发起新的任务动作。
- [ ] 对绕过受控工具的 shell／网络路径实际验证；审计无法覆盖的路径标 partial／unknown，要求完整覆盖的配置不被虚假接纳。

## 验证与证据

两个真实 Run 的污染反例、受控外部写接收端与网络／文件观测；Docker 的此结果不冒称 gVisor 强隔离。

每票交付可运行命令、最短演示步骤和输入／动作／可观察结果／证据出处。分别记录确定性实验、真实集成及本票适用的浏览器／部署／内容验收；最新实现与验证状态见下方 Comments，不由分诊标签推断验收完成。

## 贯穿本票的边界

- 随本票行为交付权限、关键事实持久化、失败与重复请求处理、所属审计事件及带新鲜度的健康／异常来源。观测出口使用有界字段允许清单。
- 一个 Run 至多一次实际 Agent 执行和一个权威终态；Result、Artifact 可用性和实际回收独立。未知结果先对账，业务重做须新 Run，阶段重试不重置累计限制。
- 页面与 API 查询同一事实；管理操作由服务端授权。提供刷新、越权和错误恢复证据，不以模拟控件或替身结果代替真实路径。

## Comments

- 2026-09-14：根据用户“接受”发布；尚未开始产品实现或验收。
- 2026-09-14：完整票已实现并独立提交：checkpoint `60cb7f4`，审查修复 `4574a6e`；取消兼容中间修复 `0585393`，后经 Spec 发现撤资格镜像的历史协议问题，独立修为 `82350d0`。固定 baseline `6b84a52`，未 amend 被审查提交；双 fresh 只读 Standards/Spec 所报问题均复核关闭。
- 2026-09-14：完整测试128/128、浏览器18/18、types/build通过；最后平台停止兼容修复追加真实SDK HTTP聚焦10/10与types。亲自目视桌面/手机边界字段。Ubuntu真实固定SDK/CLI、patched OpenSandbox server、Docker/execd与自有合成Anthropic HTTPS协议完成四个持久Run：A拒绝/B-C-D成功，A资源注入文件/配置/心跳子进程，B实际检查不继承；C合法文件与D双快照MCP兼容。全12发布端口loopback，原8sandbox/sidecar精确ID确认不存在。独立原始shell/network/proc/符号链接攻击、980次中间目录交换与子进程停止实验通过。
- 2026-09-14：审计始终partial，未观测调用结局unknown；终态后不准入新动作，已原子准入的在途动作只补合法收尾。默认Docker失败、早期sidecar公开端口及harness大小写失败均单列，不冒充最终PASS。自有远端活动资源/合成私钥已清理，仅保留镜像与共享缓存；既有五个服务healthy。实现、最短人工路径、精确SHA/镜像、验证分层和复现命令见 [实现说明](../../../docs/implementation/ticket10.md)、[验证账本](../evidence/ticket10/verification.md)、[审查记录](../evidence/ticket10/review.md)。
- 2026-09-14：未获得真实模型endpoint/model/服务端secret变量和用途计费授权，没有付费调用；gVisor未部署，生产容量/真实模型内容/用户人工综合验收未完成。上述Docker与受控模型协议证据不代替这些验收，AC保持未勾选。
