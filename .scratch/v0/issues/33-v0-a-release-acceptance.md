# 33 — 交付可复现的 v0＋A 完整验收包

**What to build:** 维护者按可运行启动／验收说明走通两类业务、A 管理路径和全部已确认故障场景，取得真实容量与证据完整性报告。

**Blocked by:** 03 — 提交响应丢失后找回同一 Run；04 — 有限等待与 SSE 断线续看；18 — 文件到期失效并保留最小未完成回收责任；19 — 显式撤销运行中授权并核验停止；22 — 外部遥测故障时继续任务并展示有界退化；25 — 通知异常并独立发现观测链失联；26 — 暂停调度或调整队首先影响待执行任务；27 — 从登记 Skills 仓库发现候选并审阅发布；28 — 保存固定能力组合并用于提交任务；29 — 调整角色权限并由服务端立即执行；30 — 功能策略控制后续文件、研究和产物复用；31 — 达到每日 token 预警阈值后生成可处置异常；32 — 在选定 Linux 部署边界复验两类工作负载

**Status:** ready-for-agent

**范围来源：** PRD + A 发布验收；2026-09-14 用户接受 v0＋A 拆分后发布。

**验收对应：** AC-28。

**管理台验收对应：** A-AC-01–12（完整发布复验）。

## Acceptance criteria

- [ ] AC-01–28、V-AO-01–12 和 A 页面／新增功能逐项链接通过／失败／未验证证据；前序票各自已完成实现与验证，本票不兜底补持久化、鉴权或测试。
- [ ] 以并发 2 和已接受限额重复两类代表性任务，报告样本数、终态分类、分阶段耗时、在途年龄、清理分布和用量完整度，不预填生产成功率／p95。
- [ ] 实际浏览器验证 A 的导航、首页层级、详情、变更影响、受限身份、刷新及错误恢复；移除模拟数据／故障注入／角色伪切换／方案切换产品入口。
- [ ] 真实模型及获准用途、固定材料／网络研究、内容核验、Docker／gVisor、资源处置、通知投递和人工 UI 验收分别记账；任一必需真实证据缺失时不宣布 v0 完成。

## 验证与证据

统一运行说明＋API 场景＋浏览器演示＋部署和测量报告；发现回归退回责任票，不以汇总表代替执行。

每票交付可运行命令、最短演示步骤和输入／动作／可观察结果／证据出处。分别记录确定性实验、真实集成及本票适用的浏览器／部署／内容验收；所有验收当前未执行。

## 贯穿本票的边界

- 随本票行为交付权限、关键事实持久化、失败与重复请求处理、所属审计事件及带新鲜度的健康／异常来源。观测出口使用有界字段允许清单。
- 一个 Run 至多一次实际 Agent 执行和一个权威终态；Result、Artifact 可用性和实际回收独立。未知结果先对账，业务重做须新 Run，阶段重试不重置累计限制。
- 页面与 API 查询同一事实；管理操作由服务端授权。提供刷新、越权和错误恢复证据，不以模拟控件或替身结果代替真实路径。

## Comments

- 2026-09-14：根据用户“接受”发布；尚未开始产品实现或验收。
