# 05 — 发布环境与模型修订并用于新任务

**What to build:** 维护者从 A 配置面板登记环境／模型、预览变更及影响、发布固定修订，调用方选择修订执行任务；停用阻止新选择。

**Blocked by:** 01 — 提交提示词并在 A 中取回已校验 JSON

**Status:** ready-for-agent

**范围来源：** PRD + A；2026-09-14 用户接受 v0＋A 拆分后发布。

**验收对应：** AC-03、AC-18。

**管理台验收对应：** A-AC-02。

## Acceptance criteria

- [ ] 修订内容不可原地改写，模型连接与 Claude runtime 分开登记；秘密只允许服务端引用，拒绝任意挂载／启动脚本及越权参数。
- [ ] 发布前展示变更摘要、影响的新任务范围及旧 Run 引用；发布／启用／停用有权限、命令身份及审计，并发变更冲突可识别。
- [ ] 新任务使用指定可用修订；旧执行清单不漂移，停用不撤销已授予的运行中能力；无获准兼容证据的 profile 不标为验证通过。

## 验证与证据

A 发布→API 新旧任务对照→停用后拒绝；重启验证修订历史和影响范围。

每票交付可运行命令、最短演示步骤和输入／动作／可观察结果／证据出处。分别记录确定性实验、真实集成及本票适用的浏览器／部署／内容验收；所有验收当前未执行。

## 贯穿本票的边界

- 随本票行为交付权限、关键事实持久化、失败与重复请求处理、所属审计事件及带新鲜度的健康／异常来源。观测出口使用有界字段允许清单。
- 一个 Run 至多一次实际 Agent 执行和一个权威终态；Result、Artifact 可用性和实际回收独立。未知结果先对账，业务重做须新 Run，阶段重试不重置累计限制。
- 页面与 API 查询同一事实；管理操作由服务端授权。提供刷新、越权和错误恢复证据，不以模拟控件或替身结果代替真实路径。

## Comments

- 2026-09-14：根据用户“接受”发布；尚未开始产品实现或验收。
- 2026-09-14：本轮 implement 已实现独立环境／模型固定修订、A 预览发布／历史／停用、真实身份和稳定命令、普通 Run 冻结选择及实际按 profile 路由。类型／构建、53 项完整确定性测试、编译进程重启已通过；浏览器配置聚焦 3 项通过。双轴审查进行中。实现与外部验收分账见 [ticket05 实施文档](../../../docs/implementation/ticket05.md) 和 [证据目录](../evidence/ticket05/implementation.md)。模型兼容性、真实模型／部署／人工验收未完成；上述综合 AC 不勾选，依赖图不改。
