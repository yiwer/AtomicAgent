# 04 — 有限等待与 SSE 断线续看

**What to build:** 短任务可在有限等待内返回结果，长任务返回身份；A 的详情通过可恢复 SSE 展示进度，断连后继续观察同一 Run。

**Blocked by:** 01 — 提交提示词并在 A 中取回已校验 JSON

**Status:** ready-for-agent

**范围来源：** PRD + A；2026-09-14 用户接受 v0＋A 拆分后发布。

**验收对应：** AC-06、AC-23。

**管理台验收对应：** A-AC-01。

## Acceptance criteria

- [ ] 提交等待最多 30 秒；HTTP 超时、关闭页面或 SSE 断开均不触发取消，也不重置任务期限。
- [ ] 事件身份、顺序及恢复游标稳定；重放可去重，过期游标明确反馈并退回权威 Run 查询。
- [ ] EOF／done 文本不能当成功；页面重新连接后显示持久终态，投影重放不重复计数，进度无原始会话、secret 或不应暴露的内部信息。

## 验证与证据

受控长短任务、SSE 断连／重放／过期游标，比较页面、API 与持久结果。

每票交付可运行命令、最短演示步骤和输入／动作／可观察结果／证据出处。分别记录确定性实验、真实集成及本票适用的浏览器／部署／内容验收；所有验收当前未执行。

## 贯穿本票的边界

- 随本票行为交付权限、关键事实持久化、失败与重复请求处理、所属审计事件及带新鲜度的健康／异常来源。观测出口使用有界字段允许清单。
- 一个 Run 至多一次实际 Agent 执行和一个权威终态；Result、Artifact 可用性和实际回收独立。未知结果先对账，业务重做须新 Run，阶段重试不重置累计限制。
- 页面与 API 查询同一事实；管理操作由服务端授权。提供刷新、越权和错误恢复证据，不以模拟控件或替身结果代替真实路径。

## Comments

- 2026-09-14：根据用户“接受”发布；尚未开始产品实现或验收。
- 2026-09-14：本轮新上下文按 implement／TDD 开始，基线 `2161c01`。有限等待、持久可恢复 SSE、A 断线续看与权限／会话边界已实现；公开HTTP/SSE聚焦、真实30秒计时、105条实际恢复观测慢读、持久重启与浏览器新增路径已验证。完整测试44/44通过；双轴审查与最终证据见 [实施记录](../../../docs/implementation/ticket04.md) 和 [证据账本](../evidence/ticket04/verification.md)。综合AC和人工确认保持未勾选，真实模型／部署条件继承01阻塞；不改变依赖图或关闭本票。
