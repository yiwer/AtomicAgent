# AtomicAgent

AtomicAgent 接收调用后端提交的 Agent 任务，利用 sandbox 中 coding agent 的能力执行任务并返回结果。

## Language

**Agent 任务**：
调用方希望平台完成的一项工作，携带提示词、初始文件及所需的执行配置；其内容不限定为软件开发。
_Avoid_: 单次模型推理、聊天消息

**调用后端**：
提交 Agent 任务并消费任务结果的自有后端服务，是当前阶段的平台调用方。
_Avoid_: 聊天用户、公开 API 租户

**独立任务**：
不自动继承其他任务的对话、工作文件或运行进程的 Agent 任务。复用前次输出时，由调用后端显式作为新任务输入提供。
_Avoid_: 持久聊天会话、共享工作区

**已登记配置**：
由平台预先登记、允许调用后端按请求选择和组合的环境、Skills 与 MCP 配置。请求中的选择和参数不自行产生额外授权。
_Avoid_: 任意启动脚本、自动授予权限

**外部只读任务**：
可以读取外部资源、在本次执行空间内生成或修改文件，但不授权修改外部业务系统的任务。
_Avoid_: 无工具任务、无文件写入任务

**任务运行（Run）**：
平台接受并独立跟踪的一次 Agent 任务，其存在不依赖提交请求的网络连接。连接断开后，任务仍可继续直至终态。
_Avoid_: HTTP 请求、Agent 对话

**任务产物（Artifact）**：
从任务执行空间转存、供调用后端下载的输出文件，其保留期独立于执行空间的寿命。
_Avoid_: sandbox 路径、聊天附件路径

**执行尝试（Attempt）**：
某个任务运行的一次具体 Agent 执行；重新接管同一执行不产生新的尝试。环境准备、模型调用或文件上传的阶段重试不等于重做整个 Agent 任务。
_Avoid_: 新业务任务、HTTP 重试

**任务结果（Result）**：
平台按调用方声明的输出要求校验后交付的结构化内容、摘要和产物清单。它与 Agent 的最后一条消息、进程退出状态分别记录。
_Avoid_: Agent 自述完成、原始对话

**终态清理**：
任务运行结束后回收其执行空间、进程与临时配置的过程；其完成情况独立于已提交的任务结果。
_Avoid_: 删除全部任务记录、断连取消

## 执行依据与资源

**任务输入材料（InputObject）**：
调用后端上传或明确引用、供任务使用的文件材料，具有独立身份、所属调用方和可用期限。
_Avoid_: 宿主机路径、任务工作副本

**输入装载记录（InputBinding）**：
某个 Run 使用哪份输入材料及其固定内容、授权和任务内位置的记录，用于区分源材料与本次执行副本。
_Avoid_: 原始下载地址、共享可写工作目录

**配置修订（ConfigurationRevision）**：
已登记配置的一个可识别版本，描述某项环境、Skill、MCP 或模型连接能力。停用配置修订与改变已有任务的执行依据是不同动作。
_Avoid_: 可变的 latest 配置、本次实际执行环境

**执行清单（ExecutionManifest）**：
某次任务最终采用的输入身份、配置修订、运行时与模型身份、输出要求及限制的固定集合。
_Avoid_: 原始请求、当前默认配置

**任务能力授权（CapabilityGrant）**：
平台授予一次任务的工具、网络、文件和模型访问范围，包含授权依据与有效边界。
_Avoid_: 提示词要求、MCP 可达性、Agent 自我授权

**执行空间占用（SandboxAllocation）**：
平台为任务持有的一份执行资源及其归属关系；资源是否仍存在需由实际观测确认。
_Avoid_: 任务运行、模型会话、资源创建请求

**资源回收单（CleanupObligation）**：
平台对一组任务资源承担的回收责任，直至有足够证据确认回收完成。发送删除请求或人工确认告警都不解除该责任。
_Avoid_: 清理日志、已发出的删除命令

## 执行证据与交付

**能力调用记录（InvocationRecord）**：
一次模型或工具调用的可观察记录，区分调用意图、执行观测和返回结果，并关联重试及父子调用。
_Avoid_: 每一次系统调用、已证明完整的 Agent 行为历史

**任务用量条目（UsageEntry）**：
具有来源、计量口径和完整程度的一份任务消耗记录，可表达 token、模型请求或供应商计费量。
_Avoid_: 无来源成本估算、未知等于零

**输出验收记录（ValidationReport）**：
平台针对任务输出契约作出的检查结果，说明检查了什么、采用什么规则以及哪些要求通过或失败。
_Avoid_: Agent 完成声明、所有研究结论正确的证明

## 审计与运行保障

**任务审计记录（AuditRecord）**：
关于任务或其相关配置、资源、访问操作的责任与结果记录，回答谁依据什么权限对什么对象做了什么，以及结果证据来自哪里。
_Avoid_: 原始模型对话、SSE 进度消息、完整思维回放

**任务进度事件（RunEvent）**：
供调用后端了解某次任务进展的授权信息视图，不独自决定任务终态或证明资源已回收。
_Avoid_: 审计原件、权威结果

**运行健康观测（HealthObservation）**：
带有观测时间、来源和覆盖范围的平台运行事实，例如任务积压、执行失联或回收逾期。缺少新观测表示未知，不能等同正常。
_Avoid_: Agent 有文字输出、HTTP 接口存活即全部健康

**运行异常（OperationalIncident）**：
一项需平台维护者或自动恢复流程处理的异常条件，关联受影响资源、触发证据和恢复证据。
_Avoid_: 每一条错误日志、已确认即已恢复
