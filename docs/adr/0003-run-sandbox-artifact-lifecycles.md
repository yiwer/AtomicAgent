---
status: accepted
---

# 分离任务运行、网络连接与执行空间的生命周期

客户端断连后，任务可以继续执行；任务进入终态后清理 sandbox。输出文件在成功交付前先转存，默认保留 24 小时，不要求保留原工作目录或 Agent 会话。

这允许调用方在连接中断后取回任务结果，也让环境清理不会破坏已承诺的产物交付。代价是平台必须独立记录任务与产物身份，并管理执行空间之外的结果存储和过期删除。
