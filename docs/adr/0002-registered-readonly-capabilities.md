---
status: accepted
---

# 首期按登记配置组合能力，外部业务资源只读

调用后端可按请求选择和组合平台登记的环境、Skills 与 MCP 配置，平台负责校验实际授权。首期任务可以在自己的执行空间中写文件，但不授权修改外部业务系统；这保留文件处理与研究任务所需能力，同时避免把任意启动配置和不可核验的外部写入纳入首期 API 承诺。

提示词中的只读要求不能替代工具权限、凭据与网络访问控制。后续若开放外部写操作，需要重新确定操作身份、授权、回执丢失与重复执行的语义。
