---
status: accepted
---

# 仅在明确阶段内重试，整项任务重做由调用方发起

平台可以在总期限和预算内重试环境准备、短暂模型错误、产物上传等明确阶段；Agent 已执行后的整项任务不自动重跑。相同幂等键找回已有 Run，调用后端需要重做业务任务时创建新任务。

这是对“自动恢复”与重复计算、结果变化和不透明费用之间的取舍。Worker 失联时先核对已有执行，不能安全接管就终止并明确失败；清理失败独立恢复，不能重做已完成业务。
