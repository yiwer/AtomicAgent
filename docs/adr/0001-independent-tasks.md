---
status: accepted
---

# 以独立任务作为调用边界

平台面向自有后端的一次任务调用，任务间不自动继承对话、工作文件或运行进程；需要复用前次输出时，由调用方显式作为新任务输入提供。这个边界符合放大一次调用的目标，也避免让首期 API 承担持久会话的续接、并发修改与环境长期占用语义。

用户在第三轮明确：客户端断连后任务可以继续，任务终态后清理 sandbox；转存的产物独立保留 24 小时。其生命周期见 [任务、连接与执行空间分离](0003-run-sandbox-artifact-lifecycles.md)。
